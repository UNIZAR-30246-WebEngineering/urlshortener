/**
 * Driving (primary) scheduled adapters for analytics.
 */
@PrimaryAdapter
package es.unizar.urlshortener.analytics.adapters.scheduling;

import org.jmolecules.architecture.hexagonal.PrimaryAdapter;
