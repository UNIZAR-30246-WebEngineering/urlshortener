/**
 * Driven (secondary) persistence adapters for links.
 */
@SecondaryAdapter
package es.unizar.urlshortener.links.adapters.persistence;

import org.jmolecules.architecture.hexagonal.SecondaryAdapter;
