/**
 * Driven (secondary) URL validation adapter for links.
 */
@SecondaryAdapter
package es.unizar.urlshortener.links.adapters.validation;

import org.jmolecules.architecture.hexagonal.SecondaryAdapter;
